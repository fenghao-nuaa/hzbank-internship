# Banking audit example

Run the complete deterministic pipeline from the repository root:

```bash
.venv/bin/python examples/banking_audit/run.py
```

The command writes `auditgraph.json`, `auditgraph.ttl`, and `auditgraph.html` into `examples/banking_audit/output/`.
